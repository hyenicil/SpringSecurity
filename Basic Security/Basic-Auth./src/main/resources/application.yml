spring:
    h2:
        console:
            enabled: true
    datasource:
        url: jdbc:h2:mem:db
    jpa:
        hibernate:
            ddl-auto: create


server:
  servlet:
    session:
        cookie:
            max-age: 15
        timeout: 15
